import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(<p>React lives!</p>, document.getElementById("root"));

// Anything that we can write in code the browser doesn't understand can also
// be written exclusively in code that it does.
// React.createElement('p', null, 'Hi')
